<p id="notice"><%= notice %></p>

<p>
<%if !@listing.pictures.blank?%>
<ol>
  <%@listing.pictures.each do |picture|%>

    <li><%= image_tag picture.url(:big).to_s %></li>
  <%end%>
<%end%>
</ol>
</p>

<p>
  <strong>Home type:</strong>
  <%= @listing.home_type %>
</p>

<p>
  <strong>Room type:</strong>
  <%= @listing.room_type %>
</p>

<p>
  <strong>Accommodates:</strong>
  <%= @listing.accommodates %>
</p>

<p>
  <strong>Address:</strong>
  <%= @listing.address %>
</p>

<p>
  <strong>City:</strong>
  <%= @listing.city %>
</p>

<%if !@listing.user.profile.nil?%>
<p>
  <strong>Host:</strong>
  <%= link_to "#{ @listing.user.profile.name }", profile_path(@listing.user.profile) %>
</p>
<%end%>

<p>
  <strong>Name:</strong>
  <%= @listing.name %>
</p>
<%= link_to 'New Reservation', new_reservation_path(listing_id: @listing.id) %>
<%if can? :update, @listing%>
<%= link_to 'Edit', edit_listing_path(@listing) %> |
<%end%>
<%= link_to 'Back', listings_path %>
